# Used by `npm test` (see tests/setup.ts). Points at a separate database so
# integration tests never touch local dev data.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/stam_test?schema=public"
PORT=3001
HOST=0.0.0.0
NODE_ENV=test
LOG_LEVEL=silent
